<%= form_with(model: ear_element, local: true) do |form| %>
  <% if ear_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ear_element.errors.count, "error") %> prohibited this ear_element from being saved:</h2>

      <ul>
        <% ear_element.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_field :img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
